const VerificationEvent = require("./VerificationEvent")

/**
 * Klasa udająca klasę łączącą się po brokerze wiadomości.
 */
class EventEmitter {

	emit(/** @type {VerificationEvent} */ event) {

	}

}

module.exports = {
  EventEmitter,
}
